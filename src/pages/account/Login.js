import React,{ Component } from "react"
import { View, Modal, Text, TouchableOpacity, Image, NativeModules, Alert, ScrollView } from "react-native"

export default class Login extends Component{
    constructor(props){
        super(props)
    }
    render(){
        return(
            <View style={{flex:1}}>
                <Text>SDFSFSD</Text>
            </View>
        )
    }

}
